{
  "devDependencies": {
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "tsc && tsc-alias",
    "setup": "ts-node win-setup.ts",
    "reset": "ts-node win-reset.ts"
  },
  "dependencies": {
    "@types/node": "^24.7.2"
  }
}
